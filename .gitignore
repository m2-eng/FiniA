cmd

.github/copilot-instructions.md

.ignore_docs/
*.backup

/.vscode/
/.venv/
**/__pycache__/

start.ps1

# Docker Compose environment-specific overrides (user configuration)
docker-compose.override.yml
docker-compose.override.yml.local

# Legacy secrets (not used anymore - Memory-Only Auth)
secrets.yaml
.secrets.yaml

# Auth Keys (if ever needed in future)
*.key
*.secret